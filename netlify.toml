[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# The static export writes 404.html; serve it for unknown paths.
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
